# xmip-core-webapi

Deprecated. Do not add new implementation here.

HTTP API operation semantics belong in `xmip-core-logic-http-api`, layered over `xmip-core-transport-http` and `xmip-core-contract-openapi`. This repository remains only as a migration marker until retirement is formally recorded.

Status: deprecated.
